# 🧭 Chrome 135 更新チェック（Stable＋コードサンプル付き）

- リリース日: 2025-04-01（Stable）
- 一次情報:
  - [Release Notes](https://developer.chrome.com/release-notes/135?hl=ja)
  - [New in Chrome 135](https://developer.chrome.com/blog/new-in-chrome-135)
  - [New in DevTools 135](https://developer.chrome.com/blog/new-in-devtools-135)
  - [New in WebGPU 135](https://developer.chrome.com/blog/new-in-webgpu-135)
  - [ChromeStatus Roadmap](https://chromestatus.com/roadmap)
  - [MDN](https://developer.mozilla.org/ja/docs/Web) / [Can I use](https://caniuse.com/)
  - [Baseline](https://web.dev/baseline?hl=ja)

---

## 🔹 全体サマリ
- HTML: **Invoker Commands（`command`/`commandfor`）**、**`<link rel="facilitated-payment">`**
- CSS: **`::scroll-button()` / `::scroll-marker`**（カルーセル UI）、**`shape()`**（自由形状のクリップパス）、**`interactivity` プロパティ**（CSS Inertness）、**`overflow-inline`/`overflow-block`**
- JavaScript/Web API: **Observable API**（リアクティブイベントストリーム）、**`Float16Array`（Baseline Newly available）**、**`setInterval()` のクランプ削除**、**`fetchLater` API**、**`highlightsFromPoint` API**
- DevTools: **Performance パネルの改善**（LCP by phase フィールドデータ、Network 依存関係ツリー、Heaviest Stack ハイライト）、**Accessibility Tree ビューのデフォルト有効化**、**Lighthouse 12.4.0**
- WebGPU: **Null bind group layouts**、**拡張ビューポート境界**、**Android 互換モード**

### 🚨 重要度の高いもの
- **Invoker Commands** により、JavaScript なしで宣言的なボタン挙動を実現（アクセシビリティ自動対応）
- **`::scroll-button()` / `::scroll-marker`** で CSS のみのカルーセル UI を構築可能
- **Observable API** によるリアクティブプログラミング（`addEventListener()` のネストを解消）
- **`Float16Array`** が Baseline Newly available に（FP16 の標準サポート）
- **DevTools Performance の依存関係ツリーインサイト**でチェーンクリティカルリクエストを検出

---

# 🧭 Chrome 135 新用語リファレンス（Glossary）

> 本Glossaryは Chrome 135 で新登場または重要化した技術用語の一覧です。

| 用語 | 概要 | 関連領域 |
|------|------|----------|
| **Invoker Commands** | `command`/`commandfor` 属性で宣言的ボタン挙動を実現。`aria-expanded` 自動対応。JavaScript 不要。 | HTML / アクセシビリティ |
| **`::scroll-button()`** | スクロール可能領域に方向ボタンを疑似要素として追加。`left`/`right` をサポート。 | CSS / UI |
| **`::scroll-marker`** | スクロールコンテナ内のフォーカス可能なマーカー疑似要素。カルーセルのページネーションに使用。 | CSS / UI |
| **`shape()`** | `clip-path`/`offset-path` で自由形状を定義。`%` や CSS 変数対応で `path()` より柔軟。 | CSS / 図形 |
| **CSS Inertness** | `interactivity` プロパティで要素を不活性化。フォーカス・編集・選択・アクセシビリティツリー可視性に影響。 | CSS / アクセシビリティ |
| **`overflow-inline`/`overflow-block`** | 論理オーバーフロープロパティ。`writing-mode` に応じて物理軸にマップ。 | CSS / 国際化 |
| **Observable API** | リアクティブプログラミングの非同期イベントストリーム処理。`addEventListener()` のネストを宣言的な変換で置き換え。 | JavaScript / Web API |
| **`Float16Array`** | IEEE FP16 数値のための型付き配列。書き込み時に自動丸め。Baseline Newly available。 | JavaScript / WebGPU |
| **`fetchLater` API** | ドキュメント破棄時・タイムアウト時・ブラウザ判断時に送信される遅延フェッチリクエスト。 | Web API / パフォーマンス |
| **`highlightsFromPoint` API** | 特定座標のカスタムハイライトを検出。ツールチップなど対話機能に使用。 | Web API / UI |
| **`NavigateEvent.sourceElement`** | ナビゲーション起点の要素（リンク・フォーム送信）を識別。 | Web API / ナビゲーション |
| **Element Reflection** | ARIA 関係属性を IDL で要素参照として反映（文字列ではなく）。 | Web API / アクセシビリティ |
| **Baseline Newly available** | 新たに横断互換になった機能の指標。`Float16Array` が該当。導入判断の目安に。 | 互換性 |
| **Origin Trial (OT)** | 実験機能の早期評価。トークン配布で実使用下の評価を可能に。 | 実験機能 |

---

## 🧩 HTML

### 1) Invoker Commands（`command`/`commandfor`）
```html
<!-- JavaScript なしで Popover を開くボタン -->
<button commandfor="my-menu" command="show-popover">
  メニューを開く
</button>
<div popover id="my-menu">
  <ul>
    <li>項目 1</li>
    <li>項目 2</li>
  </ul>
</div>

<script>
  // ブラウザが aria-expanded を自動管理
  // JavaScript による挙動追加は不要
</script>
```
> **利点**: シンプルさ・アクセシビリティの課題をブラウザが解決。共通機能を宣言的に提供。

### 2) `<link rel="facilitated-payment">`
```html
<!-- 登録済み決済クライアントへのプッシュ通知をサポート -->
<link rel="facilitated-payment" href="/payment-endpoint">
```

---

## 🎨 CSS

### 1) `::scroll-button()` と `::scroll-marker`（カルーセル UI）
```css
/* スクロール可能な領域に方向ボタンを追加 */
.carousel {
  overflow-x: auto;
  scroll-behavior: smooth;
}

.carousel::scroll-button(left) {
  position: absolute;
  left: 0;
  background: oklch(0.3 0 0 / 0.8);
  color: white;
  border: none;
  padding: 1rem;
}

.carousel::scroll-button(right) {
  position: absolute;
  right: 0;
  background: oklch(0.3 0 0 / 0.8);
  color: white;
  border: none;
  padding: 1rem;
}

/* ページネーションマーカー */
.carousel::scroll-marker-group {
  display: flex;
  gap: 0.5rem;
  justify-content: center;
  margin-block-start: 1rem;
}

.carousel::scroll-marker {
  inline-size: 0.75rem;
  block-size: 0.75rem;
  border-radius: 50%;
  background: oklch(0.6 0 0);
}

.carousel::scroll-marker:focus,
.carousel::scroll-marker:hover {
  background: oklch(0.4 0.2 240);
}
```
> **詳細**: [Carousels with CSS](https://developer.chrome.com/blog/carousels-with-css) 参照。

### 2) `shape()` 関数（自由形状のクリップパス）
```css
.custom-shape {
  /* path() より柔軟な CSS 構文で自由形状を定義 */
  clip-path: shape(
    from 0% 0%,
    line to 100% 0%,
    curve to 100% 100% via 80% 80%,
    line to 0% 100%,
    close
  );
  /* パーセンテージ・CSS 変数・math 関数をサポート */
}
```

### 3) CSS Inertness（`interactivity` プロパティ）
```css
.modal-backdrop {
  /* モーダル背後の要素を不活性化 */
  interactivity: inert;
}
```
> **効果**: フォーカス・編集・選択を無効化し、アクセシビリティツリーからも除外。

### 4) 論理オーバーフロープロパティ（`overflow-inline`/`overflow-block`）
```css
.scroll-container {
  /* 書字方向に応じて物理軸にマップ */
  overflow-inline: auto;   /* 横書きなら overflow-x、縦書きなら overflow-y */
  overflow-block: hidden;  /* 横書きなら overflow-y、縦書きなら overflow-x */
}
```

### 5) セーフエリア最大インセット変数（`safe-area-max-inset-*`）
```css
.fullscreen-content {
  /* ビューポート変更時のリレイアウトを防止 */
  padding-top: env(safe-area-max-inset-top);
  padding-bottom: env(safe-area-max-inset-bottom);
}
```

### 6) ネスト疑似要素スタイリング
```css
/* 疑似要素内の疑似要素をスタイリング */
.list-item::before::marker {
  color: oklch(0.5 0.2 240);
  font-weight: bold;
}
```

---

## ⚙️ JavaScript / Web API

### 1) Observable API（リアクティブイベントストリーム）
```js
// 従来の addEventListener() のネスト
button.addEventListener('click', (event) => {
  if (event.target.matches('.active')) {
    // 処理 1
    fetch('/api/data').then(response => {
      // 処理 2
    });
  }
});

// Observable API による宣言的な変換
// Observable はグローバルオブジェクトとして利用可能（インポート不要）

const clicks$ = new Observable(subscriber => {
  button.addEventListener('click', event => subscriber.next(event));
});

clicks$
  .filter(event => event.target.matches('.active'))
  .flatMap(() => Observable.from(fetch('/api/data')))
  .subscribe(response => {
    // 処理
  });
```

### 2) `Float16Array`（Baseline Newly available）
```js
// IEEE FP16 数値の型付き配列
const fp16 = new Float16Array([1.5, 2.7, 3.9]);
console.log(fp16[0]); // 1.5（書き込み時に自動丸め）

// WebGPU バッファとの相互運用に有用
const buffer = device.createBuffer({
  size: fp16.byteLength,
  usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST,
});
device.queue.writeBuffer(buffer, 0, fp16);
```

### 3) `setInterval()` のクランプ削除
```js
// 135 以前: 1ms 未満は 1ms にクランプされた
setInterval(() => console.log('tick'), 0); // 実際は 1ms 間隔

// 135 以降: 真の 0ms 遅延が可能
setInterval(() => console.log('tick'), 0); // 0ms 間隔
```

### 4) `fetchLater` API（遅延フェッチ）
```js
// ドキュメント破棄時・タイムアウト時・ブラウザ判断時に送信
fetchLater('/analytics', {
  method: 'POST',
  body: JSON.stringify({ event: 'page_view', timestamp: Date.now() }),
  activateAfter: 60000, // 60秒後
});
```

### 5) `highlightsFromPoint` API
```js
// 特定座標のカスタムハイライトを検出（ツールチップなど）
document.addEventListener('mousemove', (event) => {
  const highlights = CSS.highlights.highlightsFromPoint(event.clientX, event.clientY);
  if (highlights.length > 0) {
    showTooltip(highlights[0]);
  }
});
```

### 6) `NavigateEvent.sourceElement`
```js
// ナビゲーション起点の要素を識別
navigation.addEventListener('navigate', (event) => {
  console.log('Navigation triggered by:', event.sourceElement);
  // <a> タグか <form> 送信かを判別可能
});
```

### 7) Service Worker の拡張
```js
// srcdoc iframe が Service Worker クライアントを作成し、親のコントローラを継承
// Client.url プロパティは history.pushState() を無視
navigator.serviceWorker.ready.then(registration => {
  registration.active.postMessage({ type: 'get-clients' });
});

self.addEventListener('message', (event) => {
  if (event.data.type === 'get-clients') {
    clients.matchAll().then(clients => {
      clients.forEach(client => {
        console.log(client.url); // pushState による変更を反映しない
      });
    });
  }
});
```

### 8) Element Reflection（ARIA 関係属性）
```js
// ARIA 関係属性が要素参照として反映（文字列ではなく）
const input = document.querySelector('input');
const label = document.querySelector('label');

// 135 以前: 文字列で取得
console.log(input.getAttribute('aria-labelledby')); // "label-id"

// 135 以降: 要素参照で取得
console.log(input.ariaLabelledByElements); // [label]
```

---

## 🧠 AI in Chrome
- 本版での新規 AI API は目立たず。Web Speech API が `MediaStreamTrack` をサポートし、リモート音声トラックのキャプション生成が可能に。

```js
// Web Speech API で MediaStreamTrack をサポート
const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
const track = stream.getAudioTracks()[0];

const recognition = new webkitSpeechRecognition();
recognition.audioTrack = track; // MediaStreamTrack を直接指定
recognition.lang = 'ja-JP';
recognition.start();

recognition.onresult = (event) => {
  console.log('Transcript:', event.results[0][0].transcript);
};
```

---

## 🧰 DevTools

### 1) Performance パネルの改善
```text
- Origin とスクリプトリンク: Summary タブにホバー不要で関連スクリプト・origin へのリンクを表示
- LCP by phase フィールドデータ: Chrome UX Report の 75th percentile 画像タイミングを追加テーブルで比較
- Network 依存関係ツリーインサイト: チェーンクリティカルリクエストを検出し、パターン回避を推奨
- Heaviest Stack ハイライト: Main トラックの項目に Call tree / Bottom-up でホバー時にハイライト
- Duration 表示更新: "Total time" / "Self time" を統一し、"Duration (self time)" で表示
```

### 2) Accessibility Tree ビューのデフォルト有効化
```text
- Elements パネルで「Switch to Accessibility Tree view」ボタンで DOM / アクセシビリティツリーを切り替え
```

### 3) Lighthouse 12.4.0
```text
- パフォーマンス監査がパス条件下で informative としてマーク
```

### 4) その他の修正
```text
- `<select>` のアクセシビリティツリー問題と SRI Message Signature エラーの新しい問題タイプ
- Elements > Styles タブでキーボードナビゲーション時に要素をアウトライン
- Network タブで Override / Cookie 警告アイコンを再配置
```

---

## 🌐 Web API（その他）

### Fenced Frames の Automatic Beacons
```js
// クロスオリジン文書が明示的なオプトインでビーコンデータを設定可能
// （ルート・サブフレーム双方の同意が必要）
```

### SVGAElement のサポート
```html
<!-- SVG アンカー要素に rel / relList 属性 -->
<svg>
  <a href="/page" rel="noopener">
    <text>リンク</text>
  </a>
</svg>
```

### RTC Encoded Frames のタイムスタンプ
```js
// WebRTC フレームのキャプチャ・受信タイムスタンプを公開
const transformer = new TransformStream({
  transform(encodedFrame, controller) {
    console.log('Capture timestamp:', encodedFrame.captureTimestamp);
    console.log('Receive timestamp:', encodedFrame.receiveTimestamp);
    controller.enqueue(encodedFrame);
  }
});
```

### CORS リダイレクトの更新
```js
// リダイレクト時にリクエストヘッダー・ボディ・Referrer ポリシーを Fetch 仕様に準拠して更新
```

---

## 🧾 WebGPU

### 1) Null bind group layouts
```js
// 空の bind group layout を作成する必要がなくなった
const pipelineLayout = device.createPipelineLayout({
  bindGroupLayouts: [null, bindGroupLayout1, null], // null を許容し無視
});
```

### 2) 拡張ビューポート境界
```js
// レンダーターゲットの端を超えるビューポートを許容（2D UI レンダリングに有用）
passEncoder.setViewport(-10, -10, width + 20, height + 20, 0, 1);
```

### 3) Android 互換モード
```text
- chrome://flags/#enable-unsafe-webgpu で実験的な互換モードを有効化
- Vulkan 未対応デバイスで OpenGL ES バックエンドへアクセス可能
```

### 4) `maxInterStageShaderComponents` 制限の削除
```js
// maxInterStageShaderVariables との冗長性により削除
// シェーダインターフェースが簡素化
```

---

## 🧾 Origin Trial（実験機能）

### 1) Device Bound Session Credentials
```text
- セッションを単一デバイスに安全にバインド
- 定期的な更新と秘密鍵所有証明
```

### 2) Interest Invokers
```html
<!-- ホバー・キーボード・長押しで Popover 表示 -->
<button interesttarget="tooltip-id">ヒント</button>
<div popover id="tooltip-id">ツールチップ</div>

<script>
  button.addEventListener('interest', (event) => {
    console.log('InterestEvent triggered:', event.type);
  });
</script>
```

### 3) Signature-based Integrity
```text
- Ed25519 鍵ペア署名によるリソース出所検証
- CSP / Subresource Integrity を補完
```

### 4) Speculation Rules の `target_hint` フィールド
```json
{
  "prerender": [
    {
      "source": "list",
      "urls": ["/page"],
      "target_hint": "_self"
    }
  ]
}
```

---

## 🧾 非推奨 / 削除

### `navigator.xr.supportsSession` の非推奨化
```js
// 2019年9月に導入された isSessionSupported で置き換え
// const supported = await navigator.xr.supportsSession('immersive-vr'); // 非推奨
const supported = await navigator.xr.isSessionSupported('immersive-vr'); // 推奨
```

---

### 導入指針メモ
- **HTML**: Invoker Commands で JavaScript なしの宣言的 UI（アクセシビリティ自動対応）
- **CSS**: `::scroll-button()` / `::scroll-marker` でカルーセル UI を段階導入（プログレッシブエンハンスメント前提）
- **JavaScript**: Observable API でイベントハンドリングをリアクティブに（既存コードとの共存を考慮）
- **パフォーマンス**: `fetchLater` で離脱時の分析データ送信を確実に
- **DevTools**: Performance の依存関係ツリーでクリティカルリクエストチェーンを可視化し最適化
- **WebGPU**: Android 互換モードで対象デバイスを拡大（Vulkan 未対応でも実験可能）
